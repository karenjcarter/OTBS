<h1>Editing page</h1>

<%= error_messages_for :page %>

<% form_for(@page) do |f| %>
	<%= render :partial => "display_page", :locals => { :f => f , :type => "Update"} %>
<% end %>

<%= link_to 'Show', @page %> |
<%= link_to 'Back', pages_path %>
