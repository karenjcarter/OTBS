<h1>New page</h1>

<%= error_messages_for :page %>

<% form_for(@page) do |f| %>
	<%= render :partial => "display_page", :locals => { :f => f , :type => "Create"} %>
<% end %>

<%= link_to 'Back', pages_path %>
